{
  "name": "@web3os-org/web3os-apps",
  "version": "1.1.1",
  "private": true,
  "repository": "https://github.com/web3os-org/web3os-apps",
  "scripts": {
    "build": "lerna run build --concurrency=3",
    "install": "lerna exec -- pnpm install",
    "version": "lerna version",
    "version:patch": "lerna version patch",
    "version:minor": "lerna version minor",
    "version:major": "lerna version major",
    "publish:from-package": "lerna publish from-package"
  },
  "devDependencies": {
    "lerna": "^5.5.4"
  }
}
